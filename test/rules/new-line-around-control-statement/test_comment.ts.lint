if (_.noop()) {
    _.noop()
}
// single line comment
if (_.noop()) {
~~                   [0]
    _.noop()
}

if (_.noop()) {
    _.noop()
}
/**
 * Multiline comment
 */
if (_.noop()) {
~~                   [0]
    _.noop()
}

[0]: Missing blank line around control statement