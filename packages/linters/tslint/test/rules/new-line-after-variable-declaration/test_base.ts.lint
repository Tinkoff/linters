if(let i = 0) {
    _.noop();
}

for(let i = 0; i < 10; i++) {
    _.noop();
}

const a = 1;
let b;
     ~ [0]
class A {
    a() {
        let c;
        let d;
             ~ [0]
        _.noop();

        let e; _.noop();
             ~ [0]

        let f;

        _.noop();

        let o = {
            a: 1,
            b: 2,
            c: 3
        };
         ~ [0]
        _.noop();

        let j;

        // Comment
        _.noop();

        let k;
             ~ [0]
        // Comment
        _.noop();

        let u;
             ~ [0]
        // Comment
        // Comment
        _.noop();

        let q;
             ~ [0]
        /* Comment */
        _.noop();

        let x;
             ~ [0]
        /*
         * Multiline Comment
         */
        _.noop();

        let r;
             ~ [0]
        /** Js Doc */
        _.noop();
    }
}

let W;

/**
 * Js Doc
 */
class B {

}
[0]: Missing one blank line around variable declaration